{
  "name": "todo-app",
  "version": "1.0.0",
  "description": "A ToDo list application: React frontend, Express REST API, file-backed storage.",
  "type": "module",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "node scripts/dev.js",
    "start": "npm run start --workspace server",
    "build": "npm run build --workspace client",
    "test": "npm test --workspace server"
  },
  "license": "MIT",
  "engines": {
    "node": ">=20"
  }
}

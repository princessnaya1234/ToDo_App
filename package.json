{
  "name": "todo-app",
  "version": "1.0.0",
  "description": "A ToDo list application: React frontend, Express REST API, file-backed storage.",
  "type": "module",
  "private": true,
  "workspaces": [
    "shared",
    "client",
    "server"
  ],
  "scripts": {
    "dev": "node scripts/dev.js",
    "start": "npm run start --workspace server",
    "build": "npm run build --workspace client",
    "test": "npm test --workspace shared --if-present && npm test --workspace server && npm test --workspace client",
    "build:pages": "npm run build:pages --workspace client"
  },
  "license": "MIT",
  "engines": {
    "node": ">=20"
  }
}
